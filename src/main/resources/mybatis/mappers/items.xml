<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="items">
<!--   	<select id="selectAll" resultType="com.bit.myapp05.model.entity.GuestVo"> -->
  	<select id="rankAll" resultType="vo">
  		SELECT * FROM ITEMS where cate=#{cate}
  	</select>
  	
  <!-- 	<insert id="insertOne" parameterType="dto">
  		INSERT INTO GUEST VALUES(#{sabun},
  		 <choose>
		    <when test='name==""'>
		      '이름없음'
		    </when>
		    <otherwise>
		      #{name}
		    </otherwise>
		  </choose>
  		,SYSDATE,#{pay})
  	</insert> -->
  	
 
  	
  </mapper>