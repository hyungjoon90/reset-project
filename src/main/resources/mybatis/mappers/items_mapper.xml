<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="items">
<!--   	<select id="selectAll" resultType="com.bit.myapp05.model.entity.GuestVo"> -->
  	<select id="rankAll" resultType="items_vo">
  		SELECT * FROM ITEMS WHERE CATE=#{cate} ORDER BY TOT DESC LIMIT 10
  	</select>
  	<select id="rankAdd" resultType="items_vo">
		SELECT * FROM ITEMS WHERE CATE=#{cate} ORDER BY TOT DESC LIMIT 10,5
  	</select>
  	<select id="selectOne" resultType="items_vo">
  		SELECT * FROM ITEMS WHERE ITEM=#{item}
  	</select>
  	<select id="totAll" resultType="ranks_vo">
  		SELECT * FROM MYDB.RANK WHERE ITEM=#{item}
  	</select>
  	<select id="reviewAll" resultType="reviews_vo">
  		SELECT REV_NO,ITEM,IMG,NICK,AGE,SKIN,GENDER,GOOD,BAD,TIP,POP,STAR,REVIEW.NALJA FROM REVIEW INNER JOIN MEMBERS ON REVIEW.WRITER=MEMBERS.NICK WHERE ITEM=#{item} ORDER BY REV_NO LIMIT 10;
  	</select>
  	<select id="reviewAdd" resultType="reviews_vo">
  		SELECT REV_NO,ITEM,IMG,NICK,AGE,SKIN,GENDER,GOOD,BAD,TIP,POP,STAR,REVIEW.NALJA FROM REVIEW INNER JOIN MEMBERS ON REVIEW.WRITER=MEMBERS.NICK WHERE ITEM=#{item} ORDER BY REV_NO LIMIT #{review_num},5;
  	</select>
  <!-- 	<insert id="insertOne" parameterType="dto">
  		INSERT INTO GUEST VALUES(#{sabun},
  		 <choose>
		    <when test='name==""'>
		      '이름없음'
		    </when>
		    <otherwise>
		      #{name}
		    </otherwise>
		  </choose>
  		,SYSDATE,#{pay})
  	</insert> -->
  	
 
  	
  </mapper>